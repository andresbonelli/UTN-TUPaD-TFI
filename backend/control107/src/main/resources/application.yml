spring:
  application:
    name: control107

  profiles:
    active: dev

  datasource:
    url: "jdbc:h2:file:./data/control107;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1"
    driverClassName: org.h2.Driver
    username: ${DB_USER:sa}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

  security:
    user:
      name: ${SECURITY_USER_NAME:admin}
      password: ${SECURITY_USER_PWD:admin}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

logging:
  level:
    org.springframework: warn
app:
  cors:
    allowed-origins: "*"